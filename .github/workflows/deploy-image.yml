name: Remote update execution
on:
  push:
    branches:
      - dev
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: 51.250.100.218
        username: minibank
        #key_path: ${{ github.workspace }}/deploy/pkey.ppk
        key: ${{ secrets.SSH_PRIVATE_KEY_VM}}
        passphrase: anilena
        script: |
                    cd /opt/
