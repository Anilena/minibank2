name: Deploy Images on Remote VM
on:
  push:
  pull_request:
    branches: [dev]
    
jobs:
  build:
    name: Deploy
    runs-on: ubuntu-latest
    environment: dev
    steps:
    - name: executing remote ssh commands on VM
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST_VM }}
        username: minibank
        key: ${{ secrets.SSH_PRIVATE_KEY_VM }}
        passphrase: anilena
        script: |
                    docker pull docker.io/anilena2016/deploy-acount_api:lastes
                    docker pull docker.io/anilena2016/deploy-client_api:lastes
                    cd /opt/minibank
                    docker-compose down
                    docker-compose up -d
